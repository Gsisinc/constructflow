/**
 * App entry URL for "MYGSIS App" in the website navbar and footer.
 * Takes users to the app page (/Home); from there, Sign In / Sign Up go to /Login.
 */
export function getLoginUrl() {
  return '/Home';
}
